# Copy to .env.local for local development. Never commit real values — see docs/ENVIRONMENT.md.

# Base URL of the backend API (must be reachable from the browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
